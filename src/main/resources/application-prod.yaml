spring:
  datasource:
    url: jdbc:postgresql://projectv2-postgres.postgres.database.azure.com:5432/courses?currentSchema=public
    username: tomasvaneynde@projectv2-postgres
    password: Panda3212
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

cors:
  allowed-origins:
    - "https://projectv2-frontend-gqfvcxacgnhxazf5.westeurope-01.azurewebsites.net"
